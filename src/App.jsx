import { Routers } from "./helper/router";

function App() {
  // const user = useSelector((state) => state.auth.user);

  return (
    <div className="App">
      <Routers />
    </div>
  );
}

export default App;
